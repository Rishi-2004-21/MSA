{
  "name": "msa-root",
  "version": "1.0.0",
  "description": "Multi-Agent Sales Automation Root",
  "main": "MSA/server.js",
  "scripts": {
    "build": "npm install && cd MSA && npm install && npm run build",
    "start": "node MSA/server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "nodemailer": "^8.0.5",
    "openai": "^6.34.0",
    "multer": "^2.1.1",
    "xlsx": "^0.18.5",
    "concurrently": "^9.2.1",
    "marked": "^18.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
